# Pinned versions — deterministic builds on Streamlit Cloud.
# Loose ">=" ranges caused massive pip backtracking when PyPI timed out on httpcore.
streamlit==1.58.0
google-genai==1.75.0
openai==2.41.1
Pillow==12.2.0
python-docx==1.2.0
pypdf==6.13.2
pymupdf==1.27.2.3
langdetect==1.0.9
# Transitive HTTP stack pinned explicitly so the resolver never has to search:
httpx==0.28.1
httpcore==1.0.9
 
